{
    "name": "fastly-services-domain",
    "version": "0.1.0",
    "description": "AWS custom resource provider named Fastly::Services::Domain.",
    "private": true,
    "main": "dist/Fastly-Services-Domain/src/handlers.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npx tsc",
        "watch": "npx tsc -w",
        "prepack": "npm run build",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.3",
        "class-transformer": "0.3.1",
        "fastly": "^3.0.0"
    },
    "devDependencies": {
        "@types/node": "^14.0.0",
        "typescript": "^4.1.2"
    },
    "optionalDependencies": {
        "aws-sdk": "^2.1354.0"
    }
}
